Permanently deletes all twitter direct messages from a Twitter archive file

Requirements

- Twitter achive
- Twitter developer api access
- install tweepy module. Install use "pip install tweepy" in your terminal or command prompt
